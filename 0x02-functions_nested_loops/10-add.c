#include "main.h"
/**
 *add - sums two integers
 *@x: first integer to be summed
 *@y: second integer to be summed
 *Return: the sum of the teo integers
 */
int add(int x, int y)
{
	return (x + y);
}
